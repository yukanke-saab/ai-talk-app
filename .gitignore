# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # If using yarn, otherwise keep it
yarn.lock # If using npm, otherwise keep it
*.env
.env.*
!.env.example
!.env.production.example

# Build outputs
dist/
build/
*.local

# Prisma
prisma/migrations/*/*.sql # Keep the directory structure, ignore SQL
# Keep schema.prisma under version control

# Expo / React Native
.expo/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig
web-build/

# macOS
.DS_Store
*.AppleDouble
*.LSOverride
# Thumbnails
._*
# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# IDE / Editor specific
.idea/
.vscode/
*.swp
*~
